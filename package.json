{
  "name": "qr-upload-system",
  "version": "1.0.0",
  "description": "QR Code Upload System",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 